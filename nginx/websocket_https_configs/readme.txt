Nginx and WS

When using https protocol, websocket must use wss:// protocol. In this folder is working sample 
for this project, when OS version of the ngix is used and websocket is proxied through.


